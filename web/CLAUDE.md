# Web 项目说明

## Tailwind CSS v4

项目使用 Tailwind CSS v4，支持**任意数字值**写法：
- `max-w-50` → `max-width: 200px` (50 × 4)
- `w-100` → `width: 400px` (100 × 4)
- `rounded-8` → `border-radius: 32px` (8 × 4)

数字单位为 4px，不再需要 v3 的方括号语法如 `max-w-[200px]`。